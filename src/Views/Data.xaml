<Window x:Class="CombinatoricsSolverApp.Views.Data"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CombinatoricsSolverApp.Views"
        mc:Ignorable="d"
        Title="Data" Height="450" Width="800" Closing="DataWindow_Closing">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Bottom" Height="50" Background="#FF262A2F">
            <Button x:Name="DataClose" Content="Close" HorizontalAlignment="Center" Height="30" VerticalAlignment="Center" Width="45" Click="DataClose_Click"/>
        </Grid>
        <ScrollViewer DockPanel.Dock="Top">
            <DataGrid x:Name="gridData" x:FieldModifier="public" BorderThickness="0,0,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Column 1"/>
                    <DataGridTextColumn Header="Column 2"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </DockPanel>
</Window>
